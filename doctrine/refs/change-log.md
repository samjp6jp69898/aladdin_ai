# 制度檔變更流水帳（格式：- YYYY-MM-DD | 檔案 | 一句摘要 | 驗證方式）

- 2026-08-20 | CLAUDE.md（硬規則 git push 例外清單） | （使用者核准，紅區：硬規則增刪改）唯二例外改唯三例外，新增 `aladdin-toolsmith` 的 `deploy-pipeline.ts` 在 `obsidian` repo 對 `main` 分支的 push 權限，限定 tsc 結構性檢查 + 獨立對抗性覆核 agent 皆通過才 push、任一關卡沒過即回滾不 push（配合同日新增的 toolsmith 部署管線功能：企劃自助擴充 tool，sub-agent 驗證通過後自動 commit+push+reload，不回傳程式碼） | 備份至 `.claude/backups/20260820/CLAUDE.md.pre-toolsmith-push-exception`；grep 一致性檢查（commands/agents/scripts/doctrine/CLAUDE.md）確認 `permissions-worktree.md` 因範圍僅限 worktree 環境（toolsmith 操作主工作目錄非 worktree）不受影響、不需同步修改；`sync-mirrors.sh` 全綠；CLAUDE.md 85 行（上限 120）

- 2026-07-29 | scripts/notion-page-write.sh（新） | （使用者指示新增獨立腳本，非改 notion.sh）新增 update-title／append-blocks／clear-children 三個子命令，供把 skill 說明文件之類的內容直接寫入既有 Notion 頁面（沿用 notion.sh 同一把 ALD_NOTION_TOKEN，不新增 token 來源） | bash -n；實跑三個子命令對使用者新建頁面（大舞台(1)頁）清空 36 個舊區塊、改標題、附加 64 個新區塊，fetch-blocks 回讀確認 64 筆與 has_more=false

- 2026-07-21 | .claude/doctrine→obsidian/doctrine（新 symlink）+ scripts/sync-mirrors.sh + CLAUDE.md | （使用者核准，紅區：symlink↔實體目錄互換）doctrine 實體目錄搬到 obsidian/doctrine，.claude/doctrine 改為 symlink，比照 commands/agents/skills/scripts 單一來源模式（backups、settings.local.json、scheduled_tasks.lock 維持本機狀態不搬，依第 6 節規範）；sync-mirrors.sh 加入 doctrine 健檢行；CLAUDE.md 新增 symlink 慣例說明＋換機重建指令（另一台電腦 clone obsidian 後需手動 ln -sf 重建） | sync-mirrors.sh 全綠（5 個 symlink SYMLINK_OK，CLAUDE.md SYNCED）；backups/20260721-doctrine-symlink/ 備份原目錄；grep 一致性檢查無殘留舊行為描述；CLAUDE.md 80 行（上限 120）

- 2026-07-09 | scripts/notion.sh | （使用者核准）新增唯讀 `list-users` 子命令（GET /v1/users，同 get-user 呼叫模式），用於 tg-chatid-sync 對 ASK 名單查 notion_user_id | bash -n；實跑 list-users 回傳 90 位成員含 target 兩人（eling→pkh_eling@photons.com.tw, Bobo Chen KHH→pkh_yq8213@photons.com.tw）；sync-mirrors 過
- 2026-07-09 | scripts/notion.sh | list-users 補分頁支援（可選 start_cursor 參數），供全量掃描工作區成員 vs tech-users.csv 差異 | bash -n；實跑翻頁 2 頁共 89 位帶 email 成員，has_more 正確終止；sync-mirrors 過

- 2026-07-03 | ~/.claude/settings.json + CLAUDE.md + 50-letter | （使用者指示）移除 mysql MCP 整塊（root+寫權限；備份 settings.json.user.v2）；token 輪替後實測通過；CQA DB 實測**已恢復可用**（TCP 通、ai 帳號可查全服務 schema），修正 CLAUDE.md「landon_ai 是帳號非庫名」誤導措辭；信件安全節收斂為全完成 | 新 token fetch 200 + resolve-reviewer NOT_TECH；SHOW DATABASES 實測；settings.json JSON 驗證；sync-mirrors 過
- 2026-07-03 | 50-letter §三.五（新增） | v2 首跑 canary 清單（5 個盯點）+ 優化停損準則（4 不做 1 值得做） | read-back
- 2026-07-03 | .env + notion.sh + resolve-reviewer.sh + spec-fetcher.md + 3×notion-*-query.ts + sync-bug-tracker.py + 4×agent 檔 + ~/.claude/settings.json | （使用者核准）token 單源化到 .env（活躍檔零明文；.env 原有空值死行已清）；停用 superpowers plugin；移除寫死的 fable model key | notion.sh fetch 與 resolve-reviewer.sh 真 API 實測 200/NOT_TECH；py_compile/bash -n 過；殘留掃描 0；備份在 backups/20260703-fable/

- 2026-07-03 | CLAUDE.md（專案+obsidian+使用者層） | 全面重寫：15.5KB→6.3KB，硬規則+路由制，敘事抽到 doctrine/refs | read-back + sync-mirrors --check
- 2026-07-03 | .claude/doctrine/00~50 + refs/* | 建立 doctrine 體系（診斷/調度/判斷/模板/維護/信件） | 對抗審查（見下）
- 2026-07-03 | scripts/tracker.sh（新） | tracker 行級操作，禁整檔載入；next 支援 exclude | 實測 next/row/counts/set(副本)/log-fail/非法狀態
- 2026-07-03 | scripts/setup-worktree.sh（新） | /create-mr Step 4 固化（abu 多子專案 node_modules、agrabah .env.local、bootstrap 卡點判別） | bash -n + dry-run + 兩條失敗路徑實測；修多位元組插值 bug
- 2026-07-03 | scripts/resolve-reviewer.sh（新） | Notion 當前指派 → tech email；token 單源 notion.sh | bash -n（API 呼叫待首次 pipeline 實跑驗證）
- 2026-07-03 | scripts/sync-mirrors.sh（新） | CLAUDE.md 配對同步 + symlink 健檢；棄用 rsync（openrsync dry-run 靜默） | 實跑 sync + --check 全綠
- 2026-07-03 | scripts/notion.sh | token 支援 env 覆蓋；新增 comment-text 子指令（免手寫 JSON） | bash -n（API 呼叫待首次 pipeline 實跑驗證）
- 2026-07-03 | commands/create-mr/create-mr.md | v2 重寫 665→324 行：走腳本、註冊 agent 直派、契約尾行、出口查表、修 failed/7a 矛盾 | 對抗審查
- 2026-07-03 | commands/create-mrs/create-mrs.md | v2 重寫 202→69 行：移除 double-claim、SlashCommand→Skill、run-log 續命 | 對抗審查
- 2026-07-03 | （對抗審查修復輪）scripts×4、create-mr.md、create-mrs.md、CLAUDE.md、doctrine×5、agents×6 | 修 B1 token 抽取回歸（notion.sh 撤回 env-override 包裝）、B2 tracer 契約遷就定義檔原生值域+already-fixed 改定向 grep、M1~M10、MINOR×17（含 tracker set 自旋鎖、FAIL_KIND 契約、Evaluator 殘留改名） | 復核派回原審查 agent 確認；報告 refs/adversarial-review-20260703.md
- 2026-07-03 | （復核修復輪）create-mr.md、mr-pusher.md、tracker.sh | 修 NEW-1 BLOCKER：already-fixed 的 grep 偵測有 88.9% 誤判率（「已修復紀錄」是模板固定 section），改為 tracer 第 4 附加尾行 `ALREADY_FIXED: <no|yes commit=<hash>>` + 明文禁 grep 判定；M6 補完（mr-pusher bug_summary 雙形式+title 禁令）；NEW-3 鎖父目錄 mkdir -p；NEW-4 補打 update-prop 指令寫死 | 原審查 agent 最終確認（FINAL_RESULT） |
- 2026-07-03 | commands/daily-code-review.md v3 + skills/daily-code-review/{scan-workload.ts,collect-critical.ts,author-identities.json,templates/} + review-core.md + report-qa.md | 掃描/消歧/分組/prompt 生成腳本化，CRITICAL_ISSUE 改落檔聚合（_critical/ + 冪等 CSV），QA 改 Edit-only 並統一 severity 升降權（修 ced64aa5 起的兩檔矛盾），移除無效 per-call effort 宣稱與 agent 重讀 CLAUDE.md | scan 對 20260702 基準實測（16 authors、差異=pro merge 時點差已驗證）、collect 轉義/冪等/--check 實測、一致性 grep 無舊格式殘留；備份 .claude/backups/20260703-fable-dcr/
- 2026-07-03 | （dcr 對抗審查修復輪）collect-critical.ts、scan-workload.ts、daily-code-review.md、report-qa.md、qa-agent.tpl.md、review-core.md | 修 BLOCKER-1（--check 加 --batch 過濾＋Step 3 全量閘門）、MAJOR-1（三處 P0\|P1 字面歧義）、MAJOR-2（規則13 v2 探索式遺留與模板矛盾）、MAJOR-3（--skip-existing 補看 critical 檔）、MINOR-2/3/4/5（none 大小寫、Note4 例外、孤兒 prompt 清理、git TZ）；MINOR-1 記錄為設計決策不改行為 | 乾淨沙盒復測全綠（dcr-fix-verify2）；復核派回原審查 agent 待 FINAL_RESULT；報告 refs/adversarial-review-20260703-dcr.md
- 2026-07-03 | （dcr 復核修復輪）scan-workload.ts、collect-critical.ts、daily-code-review.md | 修復核新增 finding：完成判定改「最新一代報告＋critical 配對」語意（原 MAJOR-3 修法只認原始 base 名，致 _rK 完成後每次續跑重派、永不收斂、重複燒 opus）；nits：usage 補 --batch、兩處 skip 措辭更新 | 四連跑收斂實測＋復核 agent 邊角攻擊（孤兒代/代間空洞/非 skip 遞增/[DONE] 穩定性）全過，FINAL_RESULT: APPROVED；累計 10 findings 全閉環
- 2026-07-03 | （信件待辦完成輪）analyze-single-bug.md、refine-mr.md、analyze-bugs.md、create-mr.md(tracer prompt)、50-letter §三 | 9 處「Use all text in agent.md」雙載改 subagent_type 直引（每 dispatch 省上萬 token）；SlashCommand→Skill；analyze-bugs 的 tracker 整檔 Read/Edit 直改全部改走 tracker.sh（counts/next/set/log-fail，對齊 create-mrs v2 模式）；tracer 失效模式閉環第一步（create-mr Step 3 + analyze-single-bug Step 4b 派工 prompt 加「先讀 Rules『分析與失效模式』」素材行）；信件兩條待辦打勾 | 一致性 grep 殘留清零（僅 doctrine 歷史記錄合法引用）；備份 4 檔於 backups/20260703-fable-dcr/；fresh-context read-back 審查另行派出
- 2026-07-03 | skills/daily-code-review/test-dcr.sh（新）+ daily-code-review.md Note 7 | 回歸測試套件：18 案固化 v3 全部驗證行為（參數防呆/CSV 轉義冪等/P0|P1 拒收/佔位符/收斂序列/--batch 過濾/_rK 遞增），全沙盒、git 窗口自動探測（無資料自動 SKIP git 組）；修檔義務掛進 command Note 7 | 實跑 18/18 全綠；途中踩到並修復 40 號文件第 0 節記錄的 bash 3.2 全形字元陷阱（test-dcr.sh:66）
- 2026-07-03 | （round2 read-back 修復輪）analyze-bugs.md、refine-mr.md、analyze-single-bug.md | 修 read-back BLOCKER：4a LOCKED 跳過顯式寫回 exclude_list＋4e 明示同變數沿用（防殘留鎖死迴圈）；MAJOR：refine-mr header 殘留禁令句更新；MINOR×3：時間格式註記、「回到 4a」改「由 4e 判斷」、4b 後補 needs_qa_clarification 分支（set needs_qa，對齊 create-mrs 計法——狀態集合為 tracker.sh 既有，非新增） | 複核已派回原 read-back agent；報告 backups/20260703-fable-dcr/readback-review-round2.md
- 2026-07-03 | （round2 終態）analyze-single-bug.md:529 | 修複核 NEW-1：控制權交還句改為「先過外層 4b 的 Pipeline status 分流（needs_qa→set needs_qa 不是 done；其餘→4c）」，消除與 analyze-bugs 新分支的跨檔矛盾 | 原審查 agent 三輪複核 FINAL_RESULT: APPROVED（5 findings＋NEW-1 全閉環）；報告 backups/20260703-fable-dcr/readback-review-round2.md Round 2b/2c
- 2026-07-03 | scripts/pipeline-status.sh（新）+ CLAUDE.md Pipeline 速記加一行 | 「卡在 stop hook」調查：全配置層（settings×3、~/.claude.json、managed、全部 plugin 版本）證實無任何 Stop hook；實證兩個 create-mrs（FAQ-3777/3778）未卡死、在 tracer 正常時窗內——「畫面停住」是 manager 同步等 opus agent 的正常樣態。新增唯讀進度工具治「誤判卡住」 | 對兩張 live 票實測輸出正確（stage 時間線+elapsed+正常時長提示）；sync-mirrors 已同步
- 2026-07-03 | scripts/setup-worktree.sh（真檔 obsidian/scripts/）+ refs/pitfalls-worktree.md（新增踩坑 3） | 修 agrabah worktree bootstrap 致命失敗：rajah-as-symlink 標準情境 generate-all.sh 把 generated 產進主 repo，但 migrate ControlCenter 對 worktree agrabah 跑需 src/generated/services.gen.ts；原鏡像（Step 7）排在 bootstrap 後、migrate 缺 generated 失敗即 exit 故永遠補不上 → 每張 agrabah 單 SETUP_FAIL（FAQ-3777/3778 皆中）。修法：bootstrap 前新增 Step 5.5 pre-bootstrap derived-sync 先鏡像主 repo gitignored generated 進 worktree | bash -n OK；FAQ-3778 實測 SETUP_FAIL→SETUP_OK、worktree services.gen.ts 補齊 73065 行、git status 乾淨；失敗路徑（非法 repo／錯 ticket／dry-run 守衛）全確認完好；備份 backups/20260703-worktree-genfix/setup-worktree.sh.pre-genfix
- 2026-07-03 | dimensions/dim-{a,b,c,d,e,f}.md + scan-workload.ts + test-dcr.sh + daily-code-review.md Note7/8 | T2 盲區分析（885回測抽樣→40群涵蓋判定→13實例git blame回溯）核准後補強：6 份 dimension 加 Tier1 共 11 條業務語意/正確性檢查（傳值取正確實體、邊界比較運算子、enum跨repo窮盡性、匯出/顯示金額費率換算、回傳路徑、NOT NULL 齊備、SQL tie-breaker/JOIN別名/VARCHAR長度、唯一/時間驗證、rajah @Type Rate、@Permission key跨端一致）；scan-workload.ts 加唯讀 coverage-audit 子指令＋test-dcr.sh 補 T7×3（18→21案）；動機：回溯顯示漏抓主因是排程覆蓋缺口（缺口日 commit 佔 15.2%）＋語意類清單盲區，覆蓋缺口另以 coverage-audit 常態偵測（使用者已決定不補歷史缺口） | test-dcr.sh 21/21 全綠；coverage-audit 正常/失敗路徑實測（重現 04-02/04-10~12 缺口）；sync-mirrors OK；備份 backups/20260703-fable-dcr-t2/；分析產物 scratchpad/t2/（BLIND-SPOTS/round2-coverage/round3-evidence/COVERAGE-AUDIT）；fresh-context read-back 另派
- 2026-07-23 | commands/create-mrs/create-mrs.md | 新增 consecutive_skip≥8 自動提前收工（連續 not-tech/locked 跳過過多時不再問使用者，直接跳 Step 2 收工回報）| 驗證：read-back 全文確認 Step 編號與跳轉一致、grep 全庫確認無殘留舊敘述
- 2026-07-29 | obsidian/commands/dep-audit.md（新）+ obsidian/skills/dep-audit/{projects.json,dep-scan.ts,dep-worktree.sh,build-report.ts,examples/*}（新） | 新增 /dep-audit 定期套件漏洞與升級相容性流程：dep-scan.ts 走 OSV.dev + npm registry 做確定性盤點（分析單位為 (套件,版本)，因同專案可同時裝多版本）、算最小可清漏洞版本、相容性靜態預檢（engines/peer/反向 peer/dependents）；dep-worktree.sh 建 detached worktree 供實測（symlink genie/jafar/jasmine + 鏡像 .env*）；build-report.ts 併 scan/research/verify 出單檔 HTML，未研究/未實測項目強制標示。指令檔規定基準線必跑、禁把未跑步驟填 PASS | dep-scan.ts 全量實掃 10 專案 1149 組版本 20 秒完成、抽驗 protobufjs 7.5.4 CRITICAL 與 brace-expansion 無同 major 解皆與 OSV 原始資料相符；dep-worktree.sh bash -n + 四條失敗路徑 + rajah create/install/remove 實測；build-report.ts 以 Playwright 明暗雙色渲染驗證、修掉篩選 JS 誤隱藏卡片小節標題與 peerDependencies 顯示 "null" 兩個 bug；無既有制度檔衝突（grep 空）；sync-mirrors OK
- 2026-07-29 | obsidian/skills/dep-audit/{dep-worktree.sh,projects.json,build-report.ts} + obsidian/commands/dep-audit.md | /dep-audit 首次實跑後的修正：(1) dep-worktree.sh 新增 verify 與 sync-derived 兩個 action——缺 gitignored 產生碼導致基準線全紅（abu/lago vite build 炸「Could not resolve ./generated/remote.gen.ts」、agrabah 一串 TS2307），沿用 setup-worktree.sh 的 derived-sync 做法補上，create 內含且可單獨重跑；(2) rajah verify 由 `pbjs --version` 改為 pbjs static-module 產碼 + protobufjs encode/decode 往返——`pbjs --version` 印完橫幅仍 exit 1，當 smoke test 恆為 FAIL；(3) agrabah lint heap 由 8192 提到 16384（實測 8076MB 撞頂 SIGABRT，依 build-oom.md 放大）；(4) 指令檔新增「續跑」語意（同 label 重跑時 research/verify.json 合併而非覆蓋）、--only 參數、5.5b 三個實測環境限制（genie/jafar/jasmine 為 symlink 故共用庫自身依賴無法實測、eslint --fix 會改檔故基準線須先跑、lint heap 上限須兩次一致）、基準線為紅時改逐行 diff 判定；(5) build-report.ts 移除指向不可單獨執行之「Step 3」的誤導文案，改印可直接複製的續跑指令 | 全流程實跑驗證：worktree 4 repo + 157 個產生碼路徑鏡像後基準線 5/7 PASS（agrabah typecheck/lint 為既有紅燈，主 repo 同跑 753 錯確認非 worktree 造成）；agrabah protobufjs 8.0.2→8.6.6 + handlebars 4.7.8→4.7.9 後 typecheck 錯誤行集合與基準線完全一致（755=755 零新增）；bash -n + 錯誤路徑重測；sync-mirrors OK；備份 backups/20260729-depaudit/
- 2026-07-30 | scripts/setup-worktree.sh:142（真檔 obsidian/scripts/）+ refs/pitfalls-worktree.md | 修 db-seed 分類器漏判：檔頭 :17 契約本就宣告「migrate/sync-* 等 DB 步驟失敗」歸 BOOTSTRAP_PARTIAL:db-seed，但 grep 樣式只認連線層錯誤（ECONNREFUSED 等），schema 漂移型的 `migrate [Agent] error(12)`（ER_DUP_FIELDNAME）落入 else 判 SETUP_FAIL，且 bootstrap 無條件執行、與 affected_repos 無關 → 純 lago 前端單也被 agrabah DB 擋死，整批 /create-mrs 每張都會在 Step 4 陣亡（FAQ-4374 實中）。修法：樣式加 `migrate \[[A-Za-z]+\] error\(` 與 `script "(sync-all|sync-configurations)" exited` | bash -n OK；正例（FAQ-4374 真實 bootstrap.log）命中→partial、反例（Cannot find module／Segfault 假 log）未命中→仍 SETUP_FAIL；實跑 setup-worktree FAQ-4374 lago 由 SETUP_FAIL 轉 SETUP_OK BOOTSTRAP_PARTIAL:db-seed 且 derived-sync 有續跑；grep 全庫確認 commands/doctrine 各消費端本就當續行、無矛盾；備份 backups/20260730/setup-worktree.sh.pre-dbseed-classifier
- 2026-08-05 | CLAUDE.md 硬規則 | 新增「禁止用等待解決正確性問題」鐵律：不得用 sleep/setTimeout 延遲/輪詢間隔規避競態、等別行程做完、或當重試緩衝；正確性須由結構保證（additive 順序無關、樂觀鎖+影響列數檢查、型別允許暫時負值）。允許 setInterval 排程器與離線工具 DB 負載節流（recalc_post_likes.ts 批間節流）。測試碼不得靠等待時間成立。動機：MB-WRITE 多 pod 分析發現跨 pod 刪留言失敗，外部方案建議「重試加 sleep 600ms」被使用者否決並升級為鐵律（紅區變更，使用者已核准範圍：僅禁「用等待換正確性」） | 備份 backups/20260805/CLAUDE.md.before-no-sleep；grep 一致性檢查（commands/agents/doctrine）僅命中「等待使用者指令」與 Notion API 節流，無衝突；sync-mirrors.sh SYNCED；掃 cb2c587ac 全部改動檔確認無違規（recalc:112 屬允許的離線節流、test setTimeout(0) 為 macrotask yield、聚合器 setInterval 為排程器）
- 2026-08-06 | conn/（9 支腳本搬入 + README 重寫）+ tmp-sql/README.md + cqa-e2e/conn/README.md + doctrine（permissions-worktree.md、50-letter-to-future-sessions.md）+ agents（bug-tracer、bug-tracer-with-callgraph、cqa-grounder、env-preparer、test-validator）+ skills/cqa-site-usage + Rules×2 + cqa-e2e（rollup-new-registered-users lib.ts/.env×3/README、verify/*.cjs 錯誤訊息、admin-usage.md、archery-usage.md） | DB/Redis/E2E 連線腳本統一搬進 conn/ 同一層扁平目錄（tmp-sql/dev-{query,write,dump}.sh→db-dev-*.sh、tmp-sql/cqa-query.sh→db-cqa-query.sh、conn/redis/dev-query.sh→redis-dev-query.sh、cqa-e2e/conn/{platform,admin,app}-login.sh 與 cqa-e2e/verify/archery-login.sh→conn/），撞名加 db-/redis- 前綴，腳本核心邏輯（SQL/Redis 白名單、Playwright 流程）一律未動，e2e 產物仍輸出到 cqa-e2e/conn/artifacts/；doctrine 與所有引用路徑同步更新 | 9 支 bash -n 全過；db-dev-query/db-cqa-query "SELECT 1" 成功、redis-dev-query PING→PONG、platform-login.sh pk→SUCCESS、db-dev-dump.sh 實測輸出落在 conn/（測試產物已刪）；負向測試確認白名單仍擋 SET/DROP；db-dev-write.sh 依指示僅 bash -n 未實跑；額外修好 rollup-new-registered-users 對 tmp-sql/dev-query.sh 的執行期硬相依（lib.ts:70 + .env.example/.env.local/.env.dev）；obsidian/Debug/** 歷史票據紀錄刻意不改；備份 backups/20260806/*.pre-conn-move
- 2026-08-11 | skills/agrabah-local-dev/SKILL.md | 本機 platform 測試帳密改由 .env LOCAL_PLATFORM_USER/PASS 提供（使用者授權），腳本用 --env-file 載入 | grep 一致性檢查 + sync-mirrors
- 2026-08-14 | scripts/tracker.sh（新增 ensure-pending 子命令） | （使用者核准，紅區：pipeline 語意變更）telegram-dispatcher T10 claim 成功後、觸發 /create-mr 前，需要幫 tracker.md 補上/覆寫一行 pending 以滿足其 Step 0 既有存在性檢查（dispatcher 本身完全不讀這裡的狀態做決策，唯一判準仍是 Notion）；沿用 set 同一把 SETLOCK 自旋鎖，存在則只覆寫 $5=pending，不存在則安全 append 一行新資料（6 欄格式，嚴重性欄填「未知」，dispatcher 未取得該值） | bash -n OK；用 TRACKER_FILE 環境變數覆蓋指到隔離的假 tracker 檔測試：Notion 有/tracker 無的單正確新增為 pending、既有 done 狀態的單正確覆寫成 pending、兩個不同 ticket 併發呼叫皆成功寫入未互相覆蓋；grep 一致性檢查（commands/agents/scripts/doctrine/CLAUDE.md）無別處引用舊子命令列表；備份 backups/20260814/tracker.sh.pre-T7-ensure-pending；sync-mirrors.sh 全綠。⚠ 測試過程中一度誤用 Bun execFileSync 預設環境（未顯式帶 env 導致 runtime 設的 process.env.TRACKER_FILE 未傳給子行程）打到真實 tracker.md 的 FAQ-2325 行一次，該行原本已是 pending 狀態，ensure-pending 對「已是 pending」的既有列只重寫同值 $5，屬 no-op（改前改後內容逐字相同，已 diff 確認），counts 統計也與操作前一致，未造成實質影響；之後改用 shell 層級 `TRACKER_FILE=... bun run ...` 正確隔離重測通過
- 2026-08-17 | scripts/setup-worktree.sh:69（真檔 obsidian/scripts/） | （使用者核准，紅區：語意變更非單純 bug 修復）ticket_id 格式驗證從 `^FAQ-[0-9]+$` 擴大為 `^(FAQ|ALDREQ)-[0-9]+$`：telegram-dispatcher T36 需求 pipeline 要對 ALDREQ-xxx 需求單建立隔離 worktree，原本寫死只認 FAQ 會讓整個需求 pipeline 的單一 repo 自動實作路徑 100% SETUP_FAIL（review 期間實測發現）。純新增分支、不改既有 FAQ 行為 | bash -n OK；--dry-run 實測三條路徑：ALDREQ-9999999 通過驗證進入正常流程、FAQ-9999999 沿既有行為不變、WEIRD-123 仍被 SETUP_FAIL 擋下；grep 一致性檢查（commands/agents/scripts/doctrine/CLAUDE.md）確認 tracker.sh 的 FAQ-only 是刻意設計（Bug List 專用，不受影響），無其他處假設舊 regex；sync-mirrors.sh 全綠；備份 backups/20260817/setup-worktree.sh.pre-aldreq
- 2026-08-21 | commands/create-mr/create-mr.md（v2→v3）+ scripts/{setup-worktree.sh,fresh-pull.sh(新)} + agents/{bug-report-and-spec-analyst.md(新),bug-fixer-with-tests.md,solution-reviewer.md,adversarial-solution-reviewer.md(新),tdd-fidelity-reviewer.md(新)} | （使用者核准，紅區：pipeline 語意大改）Step 0 新增 fresh-pull.sh（拉新 8 個共用主 repo，包 mkdir 鎖防併發撞 .git/index.lock）；Step 0.1 ticket_id 改必填（實測 /create-mrs 與 telegram-dispatcher 兩個真實呼叫端本就永遠帶單號，無參數分支是死碼）；舊 Step 1(bug-report-analyst)+舊 Step 2(spec-fetcher) 合併一個 agent（新檔，不動舊兩個 agent 本體，因 analyze-single-bug.md 仍獨立依賴它們）；舊 Step 2.5(grounder)+舊 Step 3(tracer) 改並行派工，代價是 grounder 判 NEEDS_QA_CLARIFICATION 時丟棄同輪 tracer 分析（使用者已接受）；Step 4 worktree base 由 origin/dev 改 origin/main（main 遷移計畫進行中，目前落後 dev 534~2955 commit 不等，使用者已接受過渡期風險；連帶把 setup-worktree.sh 的 fetch/worktree-add 與 solution-reviewer.md 全部 diff 比較基準同步改 origin/main，否則 diff 範圍會炸掉）；Step 5 fixer 改 TDD（先 RED 後 GREEN，mock data 强制取自 grounding.md 實證資料，不得憑空編）；Step 6 由 1 reviewer 改 3 位平行（品質 A／對抗性 B 含硬規則機械檢查／TDD 情境符合度 C 用隔離臨時 worktree 真的重跑驗證 RED），三位皆 PASSED 才放行；舊 Step 8+9 合併。mr-pusher.md／drive-uploader-mr.md（Step 7）依使用者指示原封不動保留，故 Drive 文件與 MR 仍只含 Reviewer A 報告、仍 rebase 到 origin/dev 才 push——已知的部分緩衝但非完整修正，留待之後決定 | setup-worktree.sh/fresh-pull.sh 皆 bash -n + 實跑正常路徑（fresh-pull.sh 真跑 update.sh 17s 成功）與鎖逾時失敗路徑；create-mr.md read-back 全文確認 Step 編號與跳轉一致（新編號無「Step 3」，皆有註記說明去向）；grep 全庫確認 spec-fetcher/bug-report-analyst 舊稱僅殘留於 analyze-single-bug.md（其獨立管線，故意不動）與歷史 dated 診斷檔（不回頭改）；備份 backups/20260821-createmr-redesign/
- 2026-08-23 | scripts/notion.sh（query-datasource／fetch-blocks 兩個子命令，真檔 obsidian/scripts/）| （綠區：bug 修復，非語意變更）修 T31/T34 changelog 記錄的已知限制——兩者原本固定 page_size=100、不追 has_more/next_cursor，超過單頁筆數會靜默丟資料。改成 python3 迴圈自動追完所有分頁再合併成單一 results 陣列，回傳頂層物件形狀不變（呼叫端零改動），has_more 保證恆為 false；telegram-dispatcher 的 demand-pool-tickets.ts／spec-sufficiency-gate.ts 原本各自的 fail-loud has_more 防呆保留不刪，改當防禦性斷言（comment 同步更新說明現況）| bash -n OK；正常路徑：query-datasource 對 Bug List 整個 data source 不帶 filter 查詢，真實合併 4232 筆（≈43 頁）、has_more 正確收斂為 false；fetch-blocks 對真實頁面驗證單頁與多頁合併邏輯一致；失敗路徑：兩者對不存在的假 data_source_id/block_id 皆原樣印出 Notion API 400 錯誤訊息（不吞錯不假裝成功）；telegram-dispatcher 全量 bun test 206/206 pass；備份 backups/20260823/notion.sh.pre-pagination
- 2026-08-23 | scripts/notion.sh（同上兩個子命令，真檔 obsidian/scripts/）| （綠區：bug 修復，對上一筆的補丁）獨立 review agent 發現：分頁迴圈跑滿 1000 次防呆上限仍未見 has_more=false 時，comment 承諾「如實印出當時累積結果並保留 has_more」，但迴圈自然結束沒有觸發任何 print/sys.exit 分支，實際會印出空字串，呼叫端 JSON.parse 直接炸例外——文件與程式碼不一致（真實資料量 4232 筆 ≈43 頁不會撞到，屬極端邊界，先前驗證沒覆蓋到這條路徑）。修法：迴圈跑完後補印出 `{results: all_results, has_more: true, next_cursor: cursor}`，兌現原本的承諾 | bash -n OK；用獨立 python 腳本模擬「永遠 has_more=true」情境驗證迴圈跑滿 1000 次後正確印出合併結果（1000 筆）且 has_more=true，不是空字串；真實 API 正常路徑（query-datasource/fetch-blocks）重測仍正確
- 2026-08-25 | scripts/tg-map-chatids.sh + tg-map-chatids.test.sh（真檔 obsidian/scripts/）+ skills/tg-chatid-sync/SKILL.md | （使用者核准：診斷 GGhotss `/bug` 無回應查出白名單快取只讀一次，使用者當場要求「每次新增連接都應該重啟」）`--set` 寫入 CSV 成功（`SET_OK`）後新增自動重啟 `com.aladdin.tg-dispatch-server`（`launchctl kickstart -k gui/<uid>/...`，印 `RESTART_OK`/失敗印 `RESTART_WARN` 不影響 SET_OK 本身成敗），指令可用新增的 `TG_RESTART_CMD` 覆寫（測試用 stub，避免測試真的踢掉正式服務）；tg-chatid-sync skill、tg-auto-sync.sh 的 AUTO_HIGH、tg-monitor `/api/tg-users/assign`、/create-mr Step 0-b 四條路徑都只呼叫這支腳本本體，此處收斂一次即涵蓋全部。`--unset` 刻意不比照（移除白名單只是延後生效，非新增授權，風險方向不同）| bash -n OK 兩檔；tg-map-chatids.test.sh 全量 34/34 pass（新增 RESTART_OK/RESTART_WARN 正反兩路徑斷言，含 SET_NOOP/SET_CONFLICT/SET_ERR_NO_EMAIL 皆不觸發重啟的斷言）；grep 一致性檢查（commands/agents/scripts/doctrine/CLAUDE.md）確認 create-mr.md Step 0-b 只依賴 `SET_OK` 前綴判斷，新增的尾行不影響既有邏輯，未需修改；手動核實：實際對 `com.aladdin.tg-dispatch-server` 執行 `launchctl kickstart -k` 成功重啟（新 PID、`/health` uptime 歸零）；備份 backups/20260825/tg-map-chatids.{sh,test.sh}.pre-restart-hook
- 2026-08-25 | telegram-dispatcher/launchd/{run-cloudflare-tunnel.sh(新),com.aladdin.tg-dispatch-tunnel-cloudflare.plist(新),cloudflared-config.yml(新),run-tunnel.sh,com.aladdin.tg-dispatch-tunnel.plist} + lib/webhook-server/{health-monitor.ts,health-monitor.test.ts} | （使用者核准：這台機器裝 cloudflared 並加入既有 tunnel 當 connector）brew install cloudflared；`cloudflared tunnel login` 拿帳號層級 cert.pem；發現既有正式 tunnel `aladdin-mcp`（id 6906f8e7-e46e-43f3-abd5-b43bbaa96e3e，DNS 已指到 mcp.aladdin-assistant.cc，見 obsidian/mcps/_hosted-rollout/DEPLOY-TO-NEW-MACHINE.md 記錄的 2026-08-22 cutover）目前有活動連線（另一台機器）；一開始誤用 `tunnel create` 建了一條全新、不同 id 的 tunnel（aladdin-tg-dispatch，6e8809df...），發現用錯後已刪除（未接 DNS，無影響）。正確做法：用帳號層級 cert.pem 對既有 tunnel 現領新 credentials-file（`cloudflared tunnel token --cred-file ... aladdin-mcp`，不需要從別台機器搬檔案），寫 cloudflared-config.yml（單一 ingress 規則轉 localhost:8787）、run-cloudflare-tunnel.sh、新 launchd job，bootstrap 成功、KeepAlive 實測（kill -9 後自動重啟）、端到端驗證（本機 /ready、公網網域 /health、Telegram getWebhookInfo）全部通過。退役 ngrok：bootout 舊 job com.aladdin.tg-dispatch-tunnel、從 ~/Library/LaunchAgents/ 移除、確認無殘留 ngrok process，repo 內舊 run-tunnel.sh／plist 標記 DEPRECATED 保留供緊急回退。順帶修復 health-monitor.ts：ngrok 退役後 checkNgrokTunnelReachable 打的本機 4040 API 永遠打不到，監控會失效但不會誤報洗版（baseline 直接記不健康、之後不再變化）——改成 checkCloudflaredTunnelReachable 查 cloudflared 本機 metrics `127.0.0.1:20241/ready` 的 readyConnections 欄位，判準對齊§4.3 部署文件| bash -n 兩支腳本 OK；plutil -lint 新 plist OK；cloudflared config ingress validate OK；health-monitor.test.ts 18/18 pass（同步改寫 fake server 端點與斷言字串）；telegram-dispatcher 全量 bun test 197/197 pass；grep 一致性檢查（commands/agents/scripts/doctrine/CLAUDE.md）無其他處引用 ngrok/4040/checkNgrokTunnelReachable；真實端到端：`curl https://mcp.aladdin-assistant.cc/health` 200、`cloudflared tunnel info aladdin-mcp` 顯示本機新 connector 4 條連線皆 up、Telegram getWebhookInfo pending_update_count=0；備份 backups/20260825/{health-monitor.ts,health-monitor.test.ts,server.ts}.pre-cloudflare。⚠ 殘留觀察：`cloudflared tunnel info` 目前只看到這台機器的 connector，原本查到的「另一台機器」connector 已不在連線清單中（時序上早於本次任何操作就已間歇性掉線，研判與本次改動無關，但未進一步查證那台機器現況——不在本次任務範圍）
- 2026-08-25 | tg-monitor/{lib/services.ts,lib/ingest.ts,server.ts,public/index.html} | （使用者要求：總覽頁加更多資訊 + 可透過介面重啟服務）services.ts 的 SERVICES 登錄表把已退役的 `ngrok`（port 4040/api/tunnels）換成 `cloudflare-tunnel`（port 20241/ready，launchdLabel com.aladdin.tg-dispatch-tunnel-cloudflare），對齊同一天 telegram-dispatcher 的 cloudflared 遷移；新增 `restartService(id)`（白名單只認登錄表內、有 launchdLabel 的 id，執行 `launchctl kickstart -k gui/<uid>/<label>`，不接受任意字串當 label）。ingest.ts 的 probeOne 對應把 ngrok 專用的 tunnels 陣列解析換成 cloudflared 的 readyConnections 判準（<=0 視為 down）。server.ts 新增 `POST /api/services/restart`（本機限定，同既有 /api/tg-users/* 系列的紀律）。index.html 總覽卡片：有 launchdLabel 的服務在標題列加「重啟」按鈕（confirm 過帶 launchctl 指令說明），呼叫新 API 後 refresh(true)；順手把兩處殘留的 ngrok 字樣 comment 更新成中立敘述 | `bun build server.ts` 編譯通過（無測試套件可跑，tg-monitor 本身沒有 .test.ts）；`curl /api/overview` 實測 cloudflare-tunnel 卡片正確顯示 up + readyConnections=4 + connector id；`POST /api/services/restart` 三條路徑實測：未知 id 回 RESTART_ERR_UNKNOWN_ID、缺 id 回 RESTART_ERR_ARGS、對 dispatcher 送出後其 /health uptime 歸零證明真的重啟成功；服務本身用 kill+nohup 重啟撿到新碼（tg-monitor 沒掛 launchd，本來就是裸 bun process，未動它的部署方式）；備份 backups/20260825/tg-monitor.{services,ingest,server,index.html 對應檔}.pre-restart-ui
- 2026-08-25 | tg-monitor/{lib/webhook-status.ts(新),server.ts,public/index.html} | （使用者要求：總覽頁加 webhook 狀態卡 + 白名單/連接統計）新增 lib/webhook-status.ts：帶 30 秒內建快取查 Telegram getWebhookInfo（token 讀 /Users/user/aladdin/.env 的 TG_DISPATCH_BOT_TOKEN，不寫死），回傳 url/pending_update_count/last_error/ip_address 等，查詢本身失敗（token 讀不到／API 打不通）用 error 欄位回報而非拋例外。/api/overview 併入 `webhook` 與 `tgUsers`（複用既有 lib/tg-users.ts 的 loadConnectedUsers/loadPendingSenders 算已連接/待處理數，不重新實作）。總覽頁新增兩張卡「Telegram Webhook」「TG 連接名單」，webhook 卡特別註記「上次錯誤只在真的送達失敗時更新，修好後不會自動清空」避免誤讀成即時故障；連接名單卡的已連接/待處理數各帶連結直接跳轉既有分頁 | JS 語法用 node --check 驗證 script 區塊；`bun build server.ts` 編譯通過；重啟 tg-monitor（撞到一個 pkill pattern 沒抓到的殘留 process 佔用 8799，改用 lsof 找到真正 PID 8799 kill 掉才重啟成功，非本次改動引入的問題）後端到端實測：webhook 卡正確顯示今早的 500 last_error（含時間）與目前 pending=0；tgUsers 顯示 connectedCount=15、pendingCount=0，與 tg-users.ts 既有邏輯一致；/api/overview 其餘既有欄位（services 7 筆、pipelines）未受影響
- 2026-08-25 | obsidian/mcps/aladdin-toolsmith/{~/Library/LaunchAgents 部署,tokens.json(新，gitignored)} | （使用者要求：啟用 toolsmith）照 README「本機部署」章節走：mkdir logs/、cp plist 到 ~/Library/LaunchAgents/、plutil -lint、launchctl bootstrap，8788 /health 就緒（第 2 次輪詢）、KeepAlive 實測（kill -9 後自動拉回）。過程中發現並排除一個真假警報：經 telegram-dispatcher 的 /toolsmith proxy 打 /health 一律 401，這是 mcp-proxy.ts 的 M1 刻意設計（/health 是三支 hosted server 唯一的認證豁免端點，為避免被拿來當「前綴是否存在」的免 token 探測器，proxy 層直接攔截不轉發），不是本次部署的問題，改用本機直連 127.0.0.1:8788/health 驗證即為正確做法。真正發現的問題：README/`.mcp.json` 產生器（aladdin-ai-assistant-kit/make-starter-kit.ts）仍停留在「toolsmith 全員共用一把 TOOLSMITH_API_TOKEN」的舊設計描述，但 src/auth.ts 已於 2026-08-20 改成比照 admin/platform 的 per-user `tokens.json` 名冊（fail-closed），而這份名冊檔案從未被建立過——服務健康但沒有任何人能通過認證。使用者確認先建空名冊（`{"tokens":[]}`，暫存檔+mv 寫入，已確認 `.gitignore:40` 涵蓋不會進 git），之後要核發個人 token 再另外處理（`aladdin-kit-admin` 的產生器目前也還沒支援 toolsmith，需要另外手動照 admin/platform 的 tokens.json 格式加條目，或之後擴充產生器）| plutil -lint OK；直連 /health 200；proxy /toolsmith/health 401（設計如此，已用 mcp-proxy.ts 註解交叉核對非本次引入的問題）；直連 /mcp 帶假 token 從「名冊檔不存在」的 fail-closed 錯誤訊息，變成建立空 tokens.json 後 err log 印出「名冊已重新載入成功（0 筆條目）」+ 回應仍正確 401（0 筆條目本來就沒有任何合法 token，符合預期，不是 bug）；tg-monitor /api/overview 確認 toolsmith 卡片轉為 up、PID/uptime 正確
- 2026-08-25 | CLAUDE.md（canonical + obsidian 鏡像）+ scripts/sync-mirrors.sh + doctrine/40-maintenance-protocol.md | （使用者要求：加 AGENTS.md 給 Codex 用）動手前跑 `sync-mirrors.sh --check` 意外揪出既有 drift：obsidian/CLAUDE.md 曾被直接編輯過（違反「唯讀鏡像」紀律），內容領先 canonical 整整一個月（mtime 7/21 vs 8/20），包含一條已核准的硬規則（`git push` 三個例外新增 toolsmith deploy-pipeline，訊息裡自帶「2026-08-20 使用者核准」字樣）與一條新硬規則（禁止用 sleep/setTimeout 規避正確性問題）——canonical 完全沒有這兩條，過去一個月的 session 讀到的是舊版。先把這兩條（與其餘 mirror 領先的內容，含順手修正 2 處英文句子間漏空格的排版小 bug）合併回 canonical，才建立 `AGENTS.md`（`/Users/user/aladdin/AGENTS.md`、`obsidian/AGENTS.md` 各一份，皆為 `AGENTS.md -> CLAUDE.md` 的 symlink，讓 Codex CLI 等遵循 agents.md 開放標準的工具讀到同一份內容，不含 `.claude/commands`/`agents`/`skills` 這些 Claude Code 專屬機制，那些沒有 Codex 對應概念）。sync-mirrors.sh 的 symlink 健檢新增這兩個 AGENTS.md 的檢查；40-maintenance-protocol.md 補一條環境事實提醒「改 CLAUDE.md 前先跑 --check，不能假設 canonical 一定最新」| `diff -u` 確認合併後 canonical 只比 mirror 多 2 處空格修正、其餘逐字一致；`bash /Users/user/aladdin/scripts/sync-mirrors.sh` 執行 SYNCED，`--check` 全綠（CLAUDE.md 一致 + 7 個 symlink 全 SYMLINK_OK，含新增的 2 個 AGENTS.md）；`bash -n sync-mirrors.sh` 過；grep 全庫確認 AGENTS.md 只出現在這 3 個新改的檔案，無其他處需要同步修改；`head -2 AGENTS.md`（兩份）皆正確解析出 CLAUDE.md 內容；備份 backups/20260825/CLAUDE.md.{canonical,mirror}.pre-merge
- 2026-08-26 | create-mr.md + scripts/resume-inventory.sh（新）+ scripts/setup-worktree.sh + tg-monitor + spawn-create-mr.ts | 重試改續跑語意（紅區，使用者當日指示核准）：tg-monitor 重試按鈕 spawn 帶 --resume → create-mr Step 0.2 跑 resume-inventory.sh 盤點 Debug 產物/review 結論/mr 分支 commit 決定續跑起點（step1/2/4/5/6/7）；setup-worktree.sh 加 --keep-branch 沿用既有 mr/ 分支；階段檢核表新增「沿用上輪」狀態 | 驗證：bash -n、resume-inventory 實測 FAQ-4743（step5，A/C FAILED）與空票（step1）、setup-worktree --dry-run 兩種 flag 順序 + 無 flag 回歸、tg-monitor 重啟後 API 實測
- 2026-08-26 | create-mr.md + drive-uploader-mr.md | failed 出口強化（紅區，使用者當日指示核准）：failed 改跑 7a（上傳既有分析+審查文件，存在才傳；有 fixer diff 才編 solution.md 並標注審查未過）、7c 留言附 drive_link、新增 TG 通知 | 驗證：出口表/7a標題/7c/Pipeline Failure 段/uploader 表五處一致性 grep 通過
- 2026-08-28 | scripts/mcp-rajah-tasks.sh + doctrine/refs/pitfalls-worktree.md | （使用者當日核准「scripts 可以改」；屬 40-maintenance-protocol 第 1 節紅區的 claim/lock 協議語意變更）修 session_label 撞名導致 domain 宣告可被別人覆蓋/放掉的缺陷：domain-claim 成功時發隨機 claim_token 寫入登記並印給呼叫者，domain-release 與「同 label 的再次 claim」要求 token 相符、不符回 LABEL_COLLISION 拒絕；domain-status 只顯示 token 有無、不印 token 本身（印出來等於誰都能複製）。舊格式無 token 的既有登記一律照舊放行以維持向後相容、claim 時自動補發。檔頭補上「為什麼要帶 claim_token」與「只防呆不防惡意」的說明。過程中踩到 40-maintenance-protocol 第 0 節記載的 bash 3.2 CJK 陷阱（`$LABEL，` 變數後直接接全形逗號 → unbound variable），改用 `${LABEL}` 修正，並回頭掃描全檔確認無同類寫法 | `bash -n` 過；用 RAJAH_TASKS_DIR 指向隔離暫存目錄實測 11 條路徑：全新 claim 發 token、他人 label claim 仍 ALREADY_CLAIMED、同 label 無 token claim 被 LABEL_COLLISION 擋、同 label 帶正確 token claim 成功、release 不帶/帶錯 token 皆拒絕且 exit 1、release 帶正確 token 成功 exit 0、舊格式登記 release 只比對 label 即放行並提示、舊格式 re-claim 自動補發 token、domain-status 正確顯示 token=yes/no；對真實 _domain-claims.json 唯讀跑 domain-status 確認 18 筆既有登記全部相容（皆顯示 token=no(舊格式)）；grep commands/agents/doctrine/CLAUDE.md 確認無其他文件描述舊行為；備份 backups/20260828-sirocco/mcp-rajah-tasks.sh.before-claim-token
- 2026-08-28 | CLAUDE.md（canonical + obsidian 鏡像） | （使用者指示：幫我合併，symlink 不解決）修復 sync-mirrors.sh 報的 `CONFLICT: obsidian/CLAUDE.md 比專案根新`——這是 2026-08-25 那次 AGENTS.md 作業留下的殘餘漂移：當時的改動只落在鏡像上，canonical 停在 8/20，等於過去 8 天所有 session 常駐讀到的都是舊版。`diff -u` 確認是**單向**漂移、canonical 沒有鏡像缺的內容，且**完全沒有動到硬規則 section**，差異只有兩類：(1) 鏡像多一段 AGENTS.md 說明；(2) 4 處英文句子間漏空格的排版修正（Rule 1~4）。兩類都合併回 canonical。合併時依 40-maintenance-protocol 第 5 節「文件 vs 環境不一致以實測為準」，在 AGENTS.md 那段後面補一句現況註記：專案根的 AGENTS.md 目前不存在、使用者已裁定本輪不修復、看到 SYMLINK_MISSING 是預期內的、不要自行重建、要用 Codex 請走 obsidian/AGENTS.md——避免每個 session 常駐讀到一句與環境不符的話而去「順手修好」 | `diff -u` 確認合併後 canonical 與鏡像只差我新增的那句現況註記；`sync-mirrors.sh` 執行 SYNCED、再跑 `--check` 得 `OK: obsidian/CLAUDE.md 與專案根一致`；symlink 健檢 5 個單一來源目錄全 SYMLINK_OK，AGENTS.md 仍 SYMLINK_MISSING（已知、依指示不處理）；CLAUDE.md 87 行仍在第 4 節的 120 行上限內；grep commands/agents/doctrine 確認無其他文件描述 AGENTS.md 的舊狀態；備份 backups/20260828-sirocco/CLAUDE.md.{canonical,mirror}.pre-merge
- 2026-08-28 | obsidian/Rules/protobufjs 欄位缺席的兩種機制.md（新增）+ Rules/_index.md（登記）+ mcps/method-category-checklist.md（新增 10.5 節指標） | 記錄「回傳欄位整個不見了」的兩種成因（後端條件式賦值 vs protobufjs toJSON 丟空陣列）、repeated/scalar 判別規則與三種解法選項；起因是 fund_adjustment tool 檔頭一度把機制歸因寫錯 | 三組 dev 實測（hasOwnProperty 直打原始 message）；wiki 連結目標逐一 ls 確認存在；grep 確認 mcps/commands/agents 無殘留舊說法
- 2026-08-31 | obsidian/skills/notion-bug-assignee-report/bug-assignee-report.ts, cron/bug-report-run.sh | 推送 Telegram 邏輯內建進 .ts（預設一律推送，--no-push 可跳過），cron 包裝腳本改為單純呼叫 .ts 避免重複推送 | bash -n 通過 + 實跑一次確認三品牌皆推送成功、結束碼 0
- 2026-08-31 | CLAUDE.md（canonical + obsidian 鏡像） | （使用者當日指示：根目錄 .env 全面遷移，屬 40-maintenance-protocol 第 1 節紅區「涉及 secrets 的 .env 引用方式」，已在對話中明確核准）根目錄 /Users/user/aladdin/.env 依環境拆成 aladdin_ai/.env.{local,cqa,dev,evi,uat,prod} 六份（各自 gitignore、600 權限），硬規則 CQA grounding 段落的連線資訊來源改指 aladdin_ai/.env.cqa；同批把 aladdin_ai/conn 全部 13 支腳本、cqa-e2e/lib/env.cjs、dev-e2e/lib/env.cjs、aladdin_mcps/aladdin-toolsmith（launchd/run-server.sh + manage-tokens.ts）、telegram-dispatcher（新建 repo-local .env，遷移 run-server.sh/run-worker-agent.sh/cron/bug-report-run.sh/deploy/doctor-worker.sh/lib/webhook-server/kit-resend.ts）、tmp-sql 與 tmp/starrocks-timeout-debug 的除錯腳本全部改讀新檔案，並停用 telegram-dispatcher 的 tg-worker-agent 常駐服務（launchctl bootout，使用者指示） | grep -rn 確認 commands/agents/scripts/doctrine/CLAUDE.md 無其他處仍寫死舊路徑；sync-mirrors.sh 執行 SYNCED、--check 得 OK；4 個獨立 reviewer 交叉審查 aladdin_ai/conn 全部腳本語法與邏輯正確、.gitignore 規則逐一 git check-ignore 驗證、6 份 .env.* 權限皆 600；實測 db-cqa-query.sh/db-dev-query.sh/redis-dev-query.sh/archery-login.sh/portainer-login.sh/telegram-dispatcher/deploy/doctor-worker.sh 對真實後端 SUCCESS；備份 backups/20260831-env-split/CLAUDE.md.pre-env-split
- 2026-09-01 | agents/{mr-pusher.md,drive-uploader-mr.md} + doctrine/refs/permissions-worktree.md + commands/create-mr/create-mr.md + commands/refine-mr.md + scripts/setup-worktree.sh（註解）+ telegram-dispatcher/lib/pipeline-runner/{demand-plan-pipeline.ts,demand-plan-prompts.ts} | （使用者指示，紅區：pipeline 基準分支語意變更＋permissions-worktree 授權措辭同步）/create-mr 與需求單 pipeline 的基準分支全部統一為 origin/main：mr-pusher Step 0.5 推前 rebase 由 origin/dev 改 origin/main、`glab mr create --target-branch` 由 dev 改 main、Step 1 差異檢查改 `origin/main..HEAD`（訊息碼 BEHIND_DEV→BEHIND_MAIN）；drive-uploader-mr 全部 diff/log 比較基準改 origin/main（否則 solution.md 的 diff 範圍會含 dev 與 main 的整段落差）；permissions-worktree.md mr-pusher 例外條款的 fetch/rebase/target-branch 措辭同步；demand-plan-pipeline.ts `worktree add -B plan/<ticket>` 基準由 origin/dev 改 origin/main、prompt 描述同步。setup-worktree.sh 自 2026-08-21 起已是 origin/main，僅更新檔頭註解。已知未處理：demand-plan-pipeline.ts 建 worktree 前本就不 fetch（沿用既有行為，只換 ref）；CLAUDE.md「Git 分支策略」段仍只描述 dev/feature，未提 main；`sync-mirrors.sh --check` 揪出 CLAUDE.md 既有 DRIFT（obsidian 鏡像 08-31 比 canonical 08-25 新，內容為 08-31 已核准的 aladdin_ai 拆分事實），本次未動、待使用者裁定 | grep 全庫確認 create-mr/需求 pipeline 路徑下無 `origin/dev`／`target-branch dev` 殘留（僅歷史敘述句保留）；`bash -n setup-worktree.sh` 過；兩個 ts 以 `bun build --target bun` 解析通過（tsc 因本機缺 bun-types 無法跑，環境問題非本次引入）；備份 .claude/backups/20260901-base-main/
- 2026-09-01 | CLAUDE.md（canonical + 鏡像）+ commands/create-mr/create-mr.md + agents/{bug-report-and-spec-analyst,mr-pusher,drive-uploader-mr,solution-reviewer,adversarial-solution-reviewer,tdd-fidelity-reviewer}.md + scripts/{setup-worktree.sh,resume-inventory.sh} + doctrine/refs/permissions-worktree.md + telegram-dispatcher/lib/pipeline-runner/demand-plan-pipeline.ts | （使用者當日四點裁定）①CLAUDE.md 既有 DRIFT 依使用者裁定採鏡像（08-31 版）覆蓋 canonical 後同步；②CLAUDE.md「Git 分支策略」新增 `main` 一條：/create-mr 與需求單 pipeline 預設基準、pipeline 只在 mr/FAQ-* 上 commit 不直接動 main；③demand-plan-pipeline.ts 建 worktree 前補 `git fetch origin main`；④（紅區：pipeline 語意＋permissions 措辭）新增 `base_branch` 覆寫機制——analyst Phase A 新增 `Target Branch:` 欄位與 `TARGET_BRANCH:` 回報行（只認技術人員在 Notion 留言/內文明確寫出的分支名，多則取最晚，不猜、不自填 main/dev）；manager 新 state var `base_branch`（預設 main，resume 時從 analytics.md grep），傳給 setup-worktree.sh 新 `--base <branch>`（分支名格式白名單防 eval 注入；origin 不存在 → SETUP_FAIL 不退回 main、manager 不重試直接 failed 出口）、三位 reviewer、drive-uploader-mr、mr-pusher（fetch/rebase/`--target-branch` 三者同值）；resume-inventory.sh 新第二參數 base_branch 讓 BRANCH_COMMITS 計數基準跟分支點一致（否則 feature 領先 main 的 commit 會誤判成 fixer 已 commit）；permissions-worktree.md mr-pusher 例外改為 `<base_branch>` 措辭；Step 8 報告與 TG 通知加目標分支。已知：create-mr.md 本次前即已 407 行（超過第 4 節 400 行上限），本次再增 ~15 行，未做抽取 | setup-worktree.sh 三條路徑實測：`--base feature/20260815` dry-run 指令正確、`main;rm`／`../x` 皆 SETUP_FAIL 格式錯誤、`--base no/such-branch-zz` 對真 rajah 跑到 fetch 失敗即 SETUP_FAIL 且未留任何 worktree；resume-inventory.sh 帶合法/不合法 base 各一次；bash -n 兩腳本過；demand-plan-pipeline.ts `bun build --target bun` 過；sync-mirrors 執行 SYNCED、--check OK、CLAUDE.md 91 行；grep 確認 create-mr 路徑內 agents 已無寫死 origin/main（僅歷史敘述句）；備份 .claude/backups/20260901-base-main/（16 檔）
- 2026-09-01 | scripts/create-mr-exit-comment.sh（新）+ commands/create-mr/create-mr.md | （使用者指示，40-maintenance 第 4 節「commands 超過 400 行 → 抽可確定化步驟成 script」）Step 7c 四種非 success 出口（already_fixed / i18n_manual_handoff / needs_qa_clarification / failed）的 Notion 留言模板、AI分析 欄位值、TG 通知文字全部抽進新腳本；manager 改成一行呼叫，行首 grep `NOTION_COMMENT:` / `NOTION_AI_FIELD:` / `TG:` 三行；腳本一律 exit 0、留言失敗不影響欄位更新（比照 mr-pusher）；順手修正原模板 bootstrap 披露句會插在「連結引言：」與超連結之間的排版問題（改放主文與引言之間）。create-mr.md 由 424 行降到 393 行（回到 400 上限內）| bash -n 過；四種 status + 非法 status + 缺 drive + bootstrap-partial 各 dry-run 一次文字逐字對照原模板；Notion 實測（使用者指示「隨機三張真單」，挑 tracker 內 2026-05 完成的舊 done 單）：FAQ-3022 走 needs_qa_clarification、FAQ-2851 走 failed+bootstrap-partial、FAQ-2945 走 already_fixed，三張留言皆落地、AI分析 欄位分別變 待釐清/分析失敗/分析成功 後**皆已還原為測前值「回測完成」**；留言無法經 API 刪除，三則測試留言內文皆含「（腳本抽取後的實測留言，可忽略）」或假 commit/假 Drive 路徑可辨識；TG 路徑僅 dry-run 驗文字（實測未帶 --reviewer-email）；踩坑：測試迴圈誤用 bash 0-based 陣列於 zsh（1-based）導致單號標籤錯位一輪，已逐張複查實際 page_id 對應
- 2026-09-01 | telegram-dispatcher/deploy/sync-workers.sh（新）+ deploy/doctor-worker.sh + README.md | （使用者指示：head push 後能經 ssh 派送更新到全部 worker）新增 head 端派送腳本：讀 logs/cluster-workers.json 名冊，逐台 ssh（BatchMode）`git pull --ff-only origin main` aladdin_ai 與 telegram-dispatcher（有未 commit 已追蹤變更或分岔 → 拒絕回報，不 stash/reset）、package.json/bun.lock 有變才 bun install --frozen-lockfile、sync-mirrors --check、重啟 worker agent（/tmp/bug-analysis-locks 有 ticket 鎖預設跳過重啟，--force-restart 覆蓋）、head 端比對兩 repo HEAD 是否等於 origin/main。**不 git push**（硬規則），head 本機 main 領先 origin/main 直接拒跑。doctor-worker.sh 加「Remote Login」檢查（22 port + authorized_keys）。README 新增「head → worker 程式碼派送」段 | bash -n 主腳本與抽出的遠端片段皆過；--dry-run 正確列 landon2；真跑 landon2 因 22 port 未開回 WORKER_FAIL + exit 1（預期，一次性前提未完成）；--worker 不存在名稱 → WORKER_FAIL exit 1；遠端片段在 head 本機以 no_restart 模擬：pull no-op、symlink=ok、restart=off、head 工作區未被改動。端到端要等 landon2 開 Remote Login + ssh-copy-id 後補跑
